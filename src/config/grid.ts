export const GRID_BREAKPOINTS = {
  lg: 768, // Desktop/Tablet Landscape (Dynamic cols)
  sm: 0,   // Mobile/Tablet Portrait (5 cols)
};

export const GRID_COLS = {
  lg: 20, // Default/Min for desktop
  sm: 5,  // Fixed for mobile
};

export const TARGET_CELL_WIDTH = 60; // Target width in px for dynamic columns
export const GRID_MARGIN: [number, number] = [10, 10];
export const ROW_HEIGHT_RATIO = 1; // 1:1 aspect ratio for cells
